# reverse-patent

This repository stores the `lib-patent-reverse-layout` Codex skill.

Current bundled skill version: `v1.4.3`.

Skill entrypoint:

- `lib-patent-reverse-layout/SKILL.md`

Key update in `v1.4.3`:

- Added the LATP cathode-additive reverse-layout workflow, including LATP material parameters, cathode additive content, predictive-data marking, final table cleanup, and reverse-identification guidance.
